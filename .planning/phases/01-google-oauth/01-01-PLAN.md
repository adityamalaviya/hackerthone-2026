---
phase: 01-google-oauth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app.json
  - src/lib/appwriteMobile.ts
  - src/components/mobile/GoogleSignInButton.tsx
  - src/components/mobile/AuthScreen.tsx
  - src/components/mobile/DashboardScreen.tsx
  - src/components/mobile/MobileAuthNavigator.tsx
  - src/lib/appwriteMobile.test.ts
autonomous: true
requirements: [OAUTH-01, OAUTH-02, OAUTH-03]

must_haves:
  truths:
    - "User can tap 'Sign in with Google' displaying the specified SVG icon and initiate OAuth2 token flow"
    - "Deep link callback parses userId and secret from redirect parameters and invokes account.createSession"
    - "Protected dashboard renders user name from account.get() and provides Sign out button"
    - "Auth guards enforce redirection: unauthenticated to Auth, authenticated to Dashboard, with loading resolution"
  artifacts:
    - path: "app.json"
      provides: "Expo configuration with required custom URL scheme"
      contains: "appwrite-callback-6aa3a24400158be49594"
    - path: "src/lib/appwriteMobile.ts"
      provides: "Appwrite client and OAuth token-based authentication service"
      exports: ["client", "account", "signInWithGoogle", "signOut", "getCurrentUser"]
    - path: "src/components/mobile/GoogleSignInButton.tsx"
      provides: "Primary CTA button with official Google SVG path"
      contains: "Sign in with Google"
    - path: "src/components/mobile/DashboardScreen.tsx"
      provides: "Signed-in view with user name and sign out control"
      contains: "Sign out"
    - path: "src/components/mobile/MobileAuthNavigator.tsx"
      provides: "Auth guards and initial auth resolution"
  key_links:
    - from: "src/components/mobile/AuthScreen.tsx"
      to: "src/lib/appwriteMobile.ts"
      via: "signInWithGoogle"
      pattern: "signInWithGoogle"
    - from: "src/components/mobile/DashboardScreen.tsx"
      to: "src/lib/appwriteMobile.ts"
      via: "signOut"
      pattern: "signOut"
---

<objective>
Implement "Sign in with Google" using Appwrite Auth OAuth2 token flow, deep link session handling, navigation auth guards, and a signed-in dashboard in an Expo / React Native environment.

Purpose: Provide secure first-party OAuth authentication without storing secrets in the client application.
Output: app.json configuration, Appwrite OAuth service module, AuthScreen, DashboardScreen, Navigation guards, and automated test suite.
</objective>

<context>
Project Details:
- Project ID: 6aa3a24400158be49594
- API endpoint: https://fra.cloud.appwrite.io/v1
- OAuth provider ID: google
- Provider display name: Google
- SDK provider enum: OAuthProvider.Google (React Native)
- URL scheme: appwrite-callback-6aa3a24400158be49594
- Flow: account.createOAuth2Token -> WebBrowser.openAuthSessionAsync -> extract userId & secret -> account.createSession
- Prohibited: account.createOAuth2Session, hardcoding client secrets in client bundle.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Expo scheme and Appwrite client service</name>
  <files>app.json, src/lib/appwriteMobile.ts</files>
  <action>
    1. Create `app.json` defining scheme `appwrite-callback-6aa3a24400158be49594` and Expo config.
    2. Create `src/lib/appwriteMobile.ts` initializing Appwrite `Client` with endpoint `https://fra.cloud.appwrite.io/v1` and project `6aa3a24400158be49594`.
    3. Implement `signInWithGoogle()` using `makeRedirectUri({ preferLocalhost: true, scheme: 'appwrite-callback-6aa3a24400158be49594' })`, `account.createOAuth2Token`, `WebBrowser.openAuthSessionAsync`, parameter extraction for `userId` & `secret`, and `account.createSession({ userId, secret })`.
    4. Implement `signOut()` via `account.deleteSession("current")` and `getCurrentUser()` via `account.get()`.
    5. Follow strict TypeScript typing (no `any`, explicit return types).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>app.json and src/lib/appwriteMobile.ts compile cleanly without type errors</done>
</task>

<task type="auto">
  <name>Task 2: Build Google Sign In button, Auth screen, and Dashboard screen with auth guards</name>
  <files>src/components/mobile/GoogleSignInButton.tsx, src/components/mobile/AuthScreen.tsx, src/components/mobile/DashboardScreen.tsx, src/components/mobile/MobileAuthNavigator.tsx</files>
  <action>
    1. Create `GoogleSignInButton.tsx` with primary CTA styling, label "Sign in with Google", and the provided exact SVG path `<path fill-rule="evenodd" clip-rule="evenodd" d="M15.7034 7.91133C14.7554 7.02509 13.4903 6.54228 12.1813 6.56212C9.78605 6.56212 7.75176 8.14611 7.02642 10.279V10.2791C6.64183 11.3968 6.64183 12.6071 7.02642 13.7249H7.02979C7.75849 15.8545 9.78941 17.4385 12.1847 17.4385C13.4211 17.4385 14.4826 17.1285 15.3053 16.5809V16.5787C16.2735 15.9504 16.9348 14.9616 17.1406 13.8439H12.1813V10.3783H20.8414C20.9494 10.9802 21 11.5952 21 12.207C21 14.9443 20.002 17.2586 18.2655 18.826L18.2673 18.8274C16.7458 20.203 14.6576 21 12.1813 21C8.70985 21 5.53527 19.082 3.97666 16.043V16.043C2.67445 13.5 2.67445 10.5039 3.97666 7.96096H3.97668L3.97666 7.96094C5.53527 4.9186 8.70985 3.00061 12.1813 3.00061C14.4619 2.97415 16.6649 3.8141 18.3247 5.34188L15.7034 7.91133Z" fill="#C4C6D7"/>`.
    2. Implement `AuthScreen.tsx`: renders button, handles loading, and displays user-friendly error on failure.
    3. Implement `DashboardScreen.tsx`: displays signed-in user name (fallback to email) and "Sign out" button.
    4. Implement `MobileAuthNavigator.tsx`: resolves `account.get()` before rendering, routing authenticated sessions to Dashboard and unauthenticated to Auth.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Components render without errors, implement guards, and follow token design rules</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unit test suite for Appwrite OAuth flow and Auth Guards</name>
  <files>src/lib/appwriteMobile.test.ts</files>
  <behavior>
    - Test 1: signInWithGoogle requests OAuth token with OAuthProvider.Google and success/failure URLs matching deepLink scheme.
    - Test 2: WebBrowser result with valid secret & userId triggers account.createSession({ userId, secret }).
    - Test 3: WebBrowser failure/cancel or missing query params throws error and prevents session creation.
    - Test 4: signOut invokes account.deleteSession("current").
    - Test 5: Auth guard logic correctly determines routing based on account.get() resolution.
  </behavior>
  <action>
    Implement Vitest unit tests in `src/lib/appwriteMobile.test.ts` mocking `react-native-appwrite` and `expo-web-browser`.
  </action>
  <verify>
    <automated>npm test -- --filter=appwriteMobile</automated>
  </verify>
  <done>All automated tests pass with 100% assertion coverage on OAuth flow</done>
</task>

</tasks>

<verification>
Run `npm test` and `npm run typecheck`.
Verify that no client secrets are present anywhere in codebase.
Verify that app.json contains `"scheme": "appwrite-callback-6aa3a24400158be49594"`.
</verification>

<success_criteria>
- Appwrite OAuth2 token exchange is fully implemented and tested.
- Navigation guards resolve auth state before rendering UI.
- UI meets all styling, icon, and display requirements.
</success_criteria>
