---
phase: 01-appwrite-google-oauth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/appwrite.ts
  - src/components/auth/GoogleSignInButton.tsx
  - src/components/auth/LoginCard.tsx
  - src/pages/AuthPage.tsx
  - src/pages/OAuthSuccessPage.tsx
  - src/pages/OAuthFailurePage.tsx
  - src/pages/DashboardPage.tsx
  - src/pages/index.ts
  - src/pages/antigravity.md
  - src/App.tsx
  - src/test/oauth.test.ts
autonomous: true
requirements: [OAUTH-01, OAUTH-02, OAUTH-03, OAUTH-04]
user_setup:
  - service: appwrite
    why: "Google OAuth provider enablement in Appwrite Console"
    dashboard_config:
      - task: "Enable Google OAuth provider in Auth > Social providers"
        location: "Appwrite Console -> Project 6aa3a24400158be49594 -> Auth -> Social providers"

must_haves:
  truths:
    - "Clicking Sign in with Google initiates Account.createOAuth2Token with OAuthProvider.Google"
    - "Navigating to /auth/success reads userId and secret from query string, calls Account.createSession, and redirects to /dashboard"
    - "Navigating to /auth/failure renders an error message with a link/button returning to /auth"
    - "Accessing /dashboard while unauthenticated redirects immediately to /auth"
    - "Accessing /auth while authenticated redirects immediately to /dashboard"
    - "The mini dashboard renders the user's name (fallback to email) and a Sign out button that deletes the current session"
  artifacts:
    - path: "src/lib/appwrite.ts"
      provides: "Appwrite client, account, and OAuth token/session helper methods"
    - path: "src/components/auth/GoogleSignInButton.tsx"
      provides: "Primary CTA button with requested Google SVG icon"
    - path: "src/pages/AuthPage.tsx"
      provides: "First-party /auth page with auth guard"
    - path: "src/pages/OAuthSuccessPage.tsx"
      provides: "First-party /auth/success callback with session creation"
    - path: "src/pages/OAuthFailurePage.tsx"
      provides: "First-party /auth/failure error page"
    - path: "src/pages/DashboardPage.tsx"
      provides: "First-party /dashboard mini dashboard with auth guard and sign out"
  key_links:
    - from: "src/pages/AuthPage.tsx"
      to: "src/components/auth/GoogleSignInButton.tsx"
      via: "primary CTA render"
    - from: "src/pages/OAuthSuccessPage.tsx"
      to: "account.createSession"
      via: "session instantiation with URL credentials"
    - from: "src/App.tsx"
      to: "src/pages"
      via: "path-based route dispatch"
---

<objective>
Implement "Sign in with Google" using Appwrite Auth OAuth2 token flow (createOAuth2Token + createSession), first-party routes (/auth, /auth/success, /auth/failure, /dashboard), route guards, and signed-in dashboard.
</objective>

<execution_context>
@d:/hackerthone-2026/src/lib/appwrite.ts
@d:/hackerthone-2026/src/App.tsx
@d:/hackerthone-2026/src/components/auth/LoginCard.tsx
</execution_context>

<context>
Project Details:
- Project ID: 6aa3a24400158be49594
- API endpoint: https://fra.cloud.appwrite.io/v1
- OAuth provider ID: google
- Provider display name: Google
- SDK provider enum: OAuthProvider.Google (Web/JS)

Flow rules:
- OAuth2 token flow only: Account.createOAuth2Token, then Account.createSession in the callback route.
- Do NOT use createOAuth2Session.
- Client secrets stay in Appwrite Console, never in frontend code.
- Route guards run before rendering protected UI.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Core OAuth Token Flow & Automated Test Harness</name>
  <files>src/lib/appwrite.ts, src/test/oauth.test.ts</files>
  <behavior>
    - Test: signInWithGoogle calls account.createOAuth2Token with OAuthProvider.Google, success, and failure URLs
    - Test: handleOAuthSuccess extracts userId and secret from query string and calls account.createSession({ userId, secret })
    - Test: handleOAuthSuccess throws missing credentials error when secret or userId is absent
    - Test: signOut calls account.deleteSession({ sessionId: "current" })
  </behavior>
  <action>
    Configure Appwrite Client in src/lib/appwrite.ts with endpoint 'https://fra.cloud.appwrite.io/v1' and project ID '6aa3a24400158be49594'.
    Implement and export:
    - signInWithGoogle(): generates success/failure URLs and awaits account.createOAuth2Token({ provider: OAuthProvider.Google, success, failure })
    - handleOAuthSuccess(urlParams): validates userId and secret, awaits account.createSession({ userId, secret }), and redirects to /dashboard
    - signOut(): awaits account.deleteSession({ sessionId: "current" }) and redirects to /auth
    Write comprehensive unit tests in src/test/oauth.test.ts.
  </action>
  <verify>
    <automated>npm test -- src/test/oauth.test.ts</automated>
  </verify>
  <done>Appwrite OAuth functions and tests compile and pass.</done>
</task>

<task type="auto">
  <name>Task 2: Google Sign-In Button Component & LoginCard Integration</name>
  <files>src/components/auth/GoogleSignInButton.tsx, src/components/auth/LoginCard.tsx</files>
  <action>
    Create src/components/auth/GoogleSignInButton.tsx:
    - Primary CTA button labeled "Sign in with Google"
    - Include the exact SVG icon provided in the user request
    - Show loading spinner state while OAuth token generation initiates
    - Full dark mode support using Tailwind CSS tokens
    Update LoginCard.tsx to use this component and connect to signInWithGoogle().
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Button renders with specified SVG, label, and responsive styling.</done>
</task>

<task type="auto">
  <name>Task 3: First-Party Routes, Route Guards, and Mini Dashboard</name>
  <files>src/pages/AuthPage.tsx, src/pages/OAuthSuccessPage.tsx, src/pages/OAuthFailurePage.tsx, src/pages/DashboardPage.tsx, src/pages/index.ts, src/pages/antigravity.md, src/App.tsx</files>
  <action>
    1. Create src/pages/AuthPage.tsx:
       - Guard: await account.get(); if logged in, redirect to /dashboard.
       - If unauthenticated, render auth screen with GoogleSignInButton and links.
    2. Create src/pages/OAuthSuccessPage.tsx:
       - Read userId and secret from query string.
       - Await account.createSession({ userId, secret }).
       - Redirect to /dashboard.
       - Handle errors and redirect to /auth/failure if invalid.
    3. Create src/pages/OAuthFailurePage.tsx:
       - Render failure state with descriptive feedback and CTA to return to /auth.
    4. Create src/pages/DashboardPage.tsx:
       - Guard: await account.get(); if fails, redirect to /auth.
       - Render mini dashboard with user name (fallback to email), account details, and Sign out button.
    5. Create src/pages/index.ts and src/pages/antigravity.md.
    6. Wire route dispatcher in src/App.tsx based on window.location.pathname.
  </action>
  <verify>
    <automated>npm run build && npm test</automated>
  </verify>
  <done>All four first-party routes are wired with strict guards, tested, and passing build.</done>
</task>

</tasks>

<verification>
- `npm test` passes all tests.
- `npm run typecheck` passes with zero errors.
- `npm run build` succeeds cleanly.
</verification>

<success_criteria>
- First-party routes (/auth, /auth/success, /auth/failure, /dashboard) correctly implement token flow.
- Exact Google SVG icon and primary CTA styling match requirements.
- Route guards enforce access controls before rendering protected UI.
- No client secrets present in frontend code.
</success_criteria>
